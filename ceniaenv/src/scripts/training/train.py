import pandas as pd
from sklearn.tree import DecisionTreeClassifier
from sklearn.model_selection import train_test_split
import numpy as np
from src.data.main import X_train, X_test, y_train, y_test
from sklearn import metrics
